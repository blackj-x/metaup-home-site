{
  "name": "metaup-home",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && node scripts/check-canonical-links.mjs",
    "check:links": "node scripts/check-canonical-links.mjs",
    "fix:trailing-slashes": "node scripts/fix-internal-trailing-slashes.mjs",
    "preview": "astro preview"
  },
  "dependencies": {
    "astro": "^4.15.0"
  }
}
